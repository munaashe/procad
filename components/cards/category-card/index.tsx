import { Category } from '@/utils/types';
import React from 'react'

type Props = {
    category: Category;
}

const CategoryCard = ({
    category
}: Props) => {
    return (
        <div>
            category card
        </div>
    )
}

export default CategoryCard
