

export const metadata = {
    title: 'Portfolio | Procad Construction',
}

export default function Layout({ children }) {
    return (
        <html lang="en">
            <body >{children}</body>
        </html>
    )
}
