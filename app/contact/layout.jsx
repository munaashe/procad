

export const metadata = {
    title: 'Contact Us | Procad Construction',
}

export default function Layout({ children }) {
    return (
        <html lang="en">
            <body >{children}</body>
        </html>
    )
}
